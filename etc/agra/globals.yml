---
# 1. Global
agra_deployment_mode: docker

# 2. Grafana
enable_grafana: true
enable_ha_grafana: false
# Custom domain untuk Grafana (optional override). FALLBACK PRIORITY OTOMATIS:
#   Explicit value kamu disini (jika tidak empty string)
#   → jika kosong: pakai monitoring_vip (jika ada / tidak kosong)
#   → jika masih kosong: pakai inventory_hostname (hostname Ansible tiap node)
# Contoh isi: grafana_domain: monitor.example.com
grafana_domain: ""
grafana_database: sqlite
grafana_sqlite_sync_method: rsync
grafana_sqlite_sync_interval: 60
grafana_database_host: ""
grafana_database_port: 3306
grafana_database_name: grafana
grafana_database_user: grafana
grafana_database_ssl_mode: disable
grafana_image: grafana/grafana
grafana_tag: "11.2.0"
grafana_native_version: "11.2.0"
grafana_data_dir: /var/lib/agra/grafana
grafana_database_password: ""
grafana_admin_password: "admin"
grafana_secret_key: "CHANGE_ME_GRAFANA_SECRET_KEY_MIN_32_CHARS"

# 3. Prometheus
enable_prometheus: true
prometheus_retention_time: 15d
prometheus_retention_size: ""
prometheus_scrape_interval: 15s
prometheus_evaluation_interval: 15s
prometheus_data_dir: /var/lib/agra/prometheus
prometheus_image: prom/prometheus
prometheus_tag: v2.53.0
prometheus_native_version: "2.53.0"
expose_prometheus_via_nginx: false

# 4. Node Exporter
enable_node_exporter: true
node_exporter_port: 9100
node_exporter_image: prom/node-exporter
node_exporter_tag: v1.8.2
node_exporter_native_version: "1.8.2"
node_exporter_collectors_enabled: []
node_exporter_collectors_disabled: []
node_exporter_textfile_dir: ""

# 5. Monitoring VIP/Keepalived
monitoring_vip: ""
monitoring_vip_interface: eth0
keepalived_router_id: 51
keepalived_check_interval: 2
keepalived_fall_count: 3
keepalived_rise_count: 2
keepalived_auth_pass: ""

# 6. Nginx/TLS
enable_nginx: true
enable_https: true
# Default lokasi self-signed certificate, RSA private key, CA chain, dan Diffie-Hellman parameter di CONTROL NODE (path absolute /etc/agra/ssl).
# Di-generate secara eksplisit SEBELUM deploy via: agra certificates generate [--include-dhparam]
# Role nginx (config task block custom copy) akan OTOMATIS men-copy file dari /etc/agra/ssl ke managed host
# (target path: nginx_config_dir/ssl) JIKA file ditemukan. Jika tidak ditemukan + enable_https true,
# inline self-signed generate di managed host tetap dijalankan sebagai backward compat (creates guard).
# Untuk custom CA-signed cert: override path dibawah ke lokasi cert anda (mis. /etc/letsencrypt/live/xxx),
# ATAU taruh file agra.crt, agra.key, (opsional agra-ca.crt) di folder: /etc/agra/config/nginx/ssl/
tls_cert_path: /etc/agra/ssl/agra.crt
tls_key_path: /etc/agra/ssl/agra.key
tls_ca_path: /etc/agra/ssl/agra-ca.crt
tls_dhparam_path: /etc/agra/ssl/dhparam.pem
tls_self_signed_days_valid: 3650
tls_self_signed_cn: "{{ monitoring_vip | default('localhost') }}"

# 7. Backup & Restore
backup_destination_type: local
backup_destination_path: /var/backups/agra
backup_retention_days: 14
backup_include_prometheus_tsdb: false
backup_s3_enabled: false
backup_s3_bucket: ""
backup_s3_endpoint: ""
backup_s3_region: ""
backup_s3_access_key: ""
backup_s3_secret_key: ""
enable_scheduled_backup: false
backup_schedule_cron: "0 2 * * *"
