---
- name: Common - Include shared config tasks
  ansible.builtin.include_tasks: config.yml

- name: Common - Include docker deployment tasks (Docker-only mode)
  ansible.builtin.include_tasks: docker.yml
  when: enable_grafana | default(true) or enable_prometheus | default(true) or enable_node_exporter | default(true)
